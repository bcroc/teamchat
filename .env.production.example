# ============================================
# TeamChat Server - PRODUCTION Configuration
# ============================================
# Copy this file to .env.production and fill in ALL values
# DO NOT commit this file with real credentials!

# ---- Server Configuration ----
PORT=3001
HOST=0.0.0.0
NODE_ENV=production
API_URL=https://api.teamchat.yourcompany.com

# ---- Database (REQUIRED) ----
# Use a strong password and SSL connection in production
DATABASE_URL="postgresql://teamchat:<STRONG_PASSWORD>@db.yourcompany.com:5432/teamchat?sslmode=require"

# ---- Redis (REQUIRED) ----
# Use password authentication and TLS in production
REDIS_URL="rediss://:<REDIS_PASSWORD>@redis.yourcompany.com:6379"
REDIS_PASSWORD=<REDIS_PASSWORD>

# ---- Security (REQUIRED) ----
# Generate with: openssl rand -base64 32
# MUST be at least 32 characters
JWT_SECRET=

# ---- Rate Limiting ----
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# ---- File Storage ----
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760

# ---- CORS (REQUIRED) ----
# Comma-separated list of allowed origins
# Example: https://teamchat.yourcompany.com,https://app.teamchat.yourcompany.com
CORS_ORIGIN=

# ---- WebRTC (RECOMMENDED for production) ----
# STUN servers (comma-separated)
STUN_URLS=stun:stun.l.google.com:19302,stun:stun1.l.google.com:19302

# TURN server for NAT traversal (HIGHLY RECOMMENDED)
# Without TURN, video calls may fail for users behind firewalls
TURN_URLS=turn:turn.yourcompany.com:3478
TURN_USERNAME=teamchat
TURN_CREDENTIAL=<TURN_CREDENTIAL>

# ---- Docker Configuration ----
POSTGRES_PASSWORD=<STRONG_PASSWORD>
SERVER_PORT=3001
POSTGRES_PORT=5432
REDIS_PORT=6379
